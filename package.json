{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "A brief description of your project",
  "main": "index.js", // Entry point of your application
  "scripts": {
    "lint": "eslint .", // Command to run ESLint
    "format": "prettier --write .", // Command to run Prettier
    "start": "node index.js" // Command to start your application
  },
  "devDependencies": {
    "eslint": "^8.1.0", // ESLint dependency
    "prettier": "^2.5.1" // Prettier dependency
  }
}
